@{
    ViewBag.Title = "Dashboard";
    ViewBag.pagetitle = "Dashboard";
    ViewBag.ptitle = "Dashboard";
}

<div class="row">
    <div class="col-12">
        <div class="card cm-card">
            <div class="card-body text-center py-5">
                <div class="cm-stat-icon cm-stat-total mx-auto mb-3" style="width: 64px; height: 64px; margin-inline:auto;">
                    <i class="ri-tools-line fs-28"></i>
                </div>
                <h4 class="mb-1">Dashboard</h4>
                <p class="text-muted mb-0">Waiting for update</p>
            </div>
        </div>
    </div>
</div>