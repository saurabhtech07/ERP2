@{
    ViewData["Title"] = "Transaction";
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header align-items-center d-flex">
                <h4 class="card-title mb-0 flex-grow-1">Transaction Module</h4>
            </div>
            <div class="card-body">
                <p class="text-muted mb-0">This is the placeholder page for the Transaction module. Add your transaction screens (Sales, Purchases, etc.) here.</p>
            </div>
        </div>
    </div>
</div>